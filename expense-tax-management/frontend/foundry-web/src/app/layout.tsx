import { ClerkProvider } from "@clerk/nextjs"; import type { Metadata } from "next"; import type { ReactNode } from "react"; import { requireClerkPublishableKey } from "@/lib/clerk"; import "./styles.css"; export const metadata:Metadata={title:"ExpenseTax Foundry",description:"AI operations control plane."}; export default function Layout({children}:{children:ReactNode}){return <html lang="en"><body><ClerkProvider publishableKey={requireClerkPublishableKey(process.env.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY)}>{children}</ClerkProvider></body></html>}
