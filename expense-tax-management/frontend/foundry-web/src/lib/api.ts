import { createFoundryServiceClient } from "@expense-tax/contracts"; import type { PlatformSession } from "./session";
const headers=(s:PlatformSession)=>({authorization:`Bearer ${s.token}`});
export async function fetchProviders(s:PlatformSession){const r=await createFoundryServiceClient(s.baseUrl).GET("/internal/v1/provider-connections",{headers:headers(s)});if(!r.data)throw new Error("Providers unavailable");return r.data}
export async function fetchModes(s:PlatformSession){const r=await createFoundryServiceClient(s.baseUrl).GET("/internal/v1/ai-modes",{headers:headers(s)});if(!r.data)throw new Error("Modes unavailable");return r.data}
export async function fetchQuotaPeriods(s:PlatformSession){const r=await createFoundryServiceClient(s.baseUrl).GET("/internal/v1/quota-periods",{params:{query:{limit:100}},headers:headers(s)});if(!r.data)throw new Error("Quota periods unavailable");return r.data}
export async function fetchReconciliationQueue(s:PlatformSession){const r=await createFoundryServiceClient(s.baseUrl).GET("/internal/v1/reconciliation-queue",{params:{query:{limit:100}},headers:headers(s)});if(!r.data)throw new Error("Queue unavailable");return r.data}
export async function fetchAudit(s:PlatformSession){const r=await createFoundryServiceClient(s.baseUrl).GET("/internal/v1/foundry-audit-events",{params:{query:{limit:100}},headers:headers(s)});if(!r.data)throw new Error("Audit unavailable");return r.data}
