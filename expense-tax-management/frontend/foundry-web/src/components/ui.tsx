import type{ReactNode}from"react";export function Head({eyebrow,title,role="Catalog manager"}:{eyebrow:string;title:string;role?:string}){return <header><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1></div><span className="role"><i/>{role}</span></header>}export function Panel({title,children,wide=false}:{title:string;children:ReactNode;wide?:boolean}){return <section className={`panel ${wide?"wide":""}`}><h2>{title}</h2>{children}</section>}export function Badge({children,tone="ok"}:{children:ReactNode;tone?:"ok"|"warn"|"bad"}){return <span className={`badge ${tone}`}>{children}</span>}
