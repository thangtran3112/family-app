"use client";import{Activity,Boxes,Database,FileClock,GitBranch,ShieldAlert}from"lucide-react";import Link from"next/link";import{usePathname}from"next/navigation";import type{ReactNode}from"react";const nav=[["/providers","Providers",Database],["/models","Models",Boxes],["/modes","Modes",GitBranch],["/quotas","Quotas",Activity],["/reconciliation","Reconciliation",ShieldAlert],["/audit","Audit",FileClock]]as const;export function Shell({children}:{children:ReactNode}){const path=usePathname();return <div className="shell"><aside><Link className="brand" href="/providers"><span>F</span>ExpenseTax Foundry</Link><nav>{nav.map(([href,label,Icon])=><Link key={href} href={href} aria-current={path.startsWith(href)?"page":undefined}><Icon size={17}/>{label}</Link>)}</nav><p>Platform tokens only<br/>Customer content forbidden</p></aside><main>{children}</main></div>}
