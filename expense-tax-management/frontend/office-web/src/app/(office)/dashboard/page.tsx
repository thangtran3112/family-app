import { Metric, PageHead, Panel, Status } from "@/components/ui";
import { expenses, report } from "@/lib/fixtures";
export default function Dashboard() { return <><PageHead eyebrow="Business overview" title="Books that explain themselves."><p>Dense review without provider or filing controls.</p></PageHead><div className="metrics"><Metric label="Ready expenses" value="$12,840.72" note={`${report.ready} reviewed · USD base`} /><Metric label="Needs review" value={String(report.review)} note="4 missing treatment · 3 unreviewed" warn /><Metric label="OCR allowance" value="18 / 20" note="UTC reset · 1 admitted" /></div><Panel title="Recent expenses"><table><thead><tr><th>Date</th><th>Merchant</th><th>Project</th><th>Status</th><th>Amount</th></tr></thead><tbody>{expenses.map(row => <tr key={row.id}><td>{row.date}</td><td>{row.merchant}</td><td>{row.project}</td><td><Status tone={row.status.includes("Foreign") ? "warn" : "ok"}>{row.status}</Status></td><td className="money">{row.amount}</td></tr>)}</tbody></table></Panel></>; }
