"use client";
import { useDeferredValue, useState } from "react";
import { PageHead, Panel, Status } from "@/components/ui";
import { filterExpenses } from "@/lib/fixtures";
export default function Expenses() { const [query,setQuery]=useState(""); const rows=filterExpenses(useDeferredValue(query)); return <><PageHead eyebrow="Ledger" title="Every expense, one scope."><p>Exact filters, deterministic sort, cursor pagination.</p></PageHead><Panel title="Expense ledger"><div className="toolbar"><label>Search visible fields<input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Merchant, project, status" /></label><button>Date range</button><button>Review state</button><button>Project</button></div><table><thead><tr><th aria-sort="descending">Date</th><th>Merchant</th><th>Project</th><th>Tax</th><th>Status</th><th>Amount</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td>{row.date}</td><td>{row.merchant}</td><td>{row.project}</td><td>{row.tax}</td><td><Status tone={row.status.includes("review")?"warn":"ok"}>{row.status}</Status></td><td className="money">{row.amount}</td></tr>)}</tbody></table>{rows.length===0&&<div className="empty">No results. Filters remain editable.</div>}</Panel></>; }
