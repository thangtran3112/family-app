include:
  - path: docker-compose.common.yml
  - path: ../expense-tax-management/docker-compose.yml

# This file is a convenience wrapper that merges the shared stack with the
# expense-tax-management app overlay. Prefer running:
#   docker compose -f infrastructure/docker-compose.common.yml -f expense-tax-management/docker-compose.yml up -d
# from the family-app root, or
#   docker compose -f ../infrastructure/docker-compose.common.yml -f docker-compose.yml up -d
# from expense-tax-management/.
